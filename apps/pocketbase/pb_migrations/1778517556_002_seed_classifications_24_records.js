/// <reference path="../pb_data/types.d.ts" />
migrate((app) => {
  const collection = app.findCollectionByNameOrId("classifications");

  const record0 = new Record(collection);
    record0.set("label", "ACTIVE RECORD");
    record0.set("category", "status");
    record0.set("description", "Published, in active circulation");
  try {
    app.save(record0);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record1 = new Record(collection);
    record1.set("label", "FORTHCOMING TRANSMISSION");
    record1.set("category", "status");
    record1.set("description", "Coming soon, not yet published");
  try {
    app.save(record1);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record2 = new Record(collection);
    record2.set("label", "PARTIAL RECONSTRUCTION");
    record2.set("category", "completeness");
    record2.set("description", "Recovered from damaged/incomplete sources");
  try {
    app.save(record2);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record3 = new Record(collection);
    record3.set("label", "RECOVERED");
    record3.set("category", "completeness");
    record3.set("description", "Successfully recovered and verified");
  try {
    app.save(record3);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record4 = new Record(collection);
    record4.set("label", "UNDER RESTORATION");
    record4.set("category", "completeness");
    record4.set("description", "Currently being restored/reconstructed");
  try {
    app.save(record4);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record5 = new Record(collection);
    record5.set("label", "INCOMPLETE TRANSLATION");
    record5.set("category", "completeness");
    record5.set("description", "Translation uncertain or partial");
  try {
    app.save(record5);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record6 = new Record(collection);
    record6.set("label", "RESTRICTED CIRCULATION");
    record6.set("category", "security");
    record6.set("description", "Limited access, sensitive content");
  try {
    app.save(record6);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record7 = new Record(collection);
    record7.set("label", "UNSAFE CONSTRUCTION EXAMPLES");
    record7.set("category", "security");
    record7.set("description", "Dangerous linguistic forms");
  try {
    app.save(record7);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record8 = new Record(collection);
    record8.set("label", "FOUNDATIONAL RULE");
    record8.set("category", "security");
    record8.set("description", "Core safety protocol");
  try {
    app.save(record8);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record9 = new Record(collection);
    record9.set("label", "CONDITIONAL USE");
    record9.set("category", "security");
    record9.set("description", "Permitted only in specific contexts");
  try {
    app.save(record9);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record10 = new Record(collection);
    record10.set("label", "INSTITUTIONAL PROTOCOLS");
    record10.set("category", "security");
    record10.set("description", "Administrative procedures");
  try {
    app.save(record10);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record11 = new Record(collection);
    record11.set("label", "MUNICIPAL LEXICON");
    record11.set("category", "security");
    record11.set("description", "Municipal/administrative language");
  try {
    app.save(record11);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record12 = new Record(collection);
    record12.set("label", "SHORELINE RECORD");
    record12.set("category", "security");
    record12.set("description", "Coastal/environmental documentation");
  try {
    app.save(record12);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record13 = new Record(collection);
    record13.set("label", "ARCHIVAL SYSTEMS");
    record13.set("category", "security");
    record13.set("description", "Archive management procedures");
  try {
    app.save(record13);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record14 = new Record(collection);
    record14.set("label", "RITUAL CASE");
    record14.set("category", "security");
    record14.set("description", "Ritual/ceremonial language");
  try {
    app.save(record14);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record15 = new Record(collection);
    record15.set("label", "FUNERARY CASE");
    record15.set("category", "security");
    record15.set("description", "Funeral/memorial protocols");
  try {
    app.save(record15);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record16 = new Record(collection);
    record16.set("label", "SEASONAL CASE");
    record16.set("category", "security");
    record16.set("description", "Seasonal/temporal protocols");
  try {
    app.save(record16);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record17 = new Record(collection);
    record17.set("label", "STRUCTURAL FEATURE");
    record17.set("category", "security");
    record17.set("description", "Linguistic structure");
  try {
    app.save(record17);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record18 = new Record(collection);
    record18.set("label", "CRITICAL SAFETY RULES");
    record18.set("category", "security");
    record18.set("description", "Essential safety protocols");
  try {
    app.save(record18);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record19 = new Record(collection);
    record19.set("label", "SPEECH PROTOCOLS");
    record19.set("category", "security");
    record19.set("description", "Speaking guidelines");
  try {
    app.save(record19);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record20 = new Record(collection);
    record20.set("label", "WEATHER CORRELATION");
    record20.set("category", "security");
    record20.set("description", "Weather-related phenomena");
  try {
    app.save(record20);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record21 = new Record(collection);
    record21.set("label", "LINGUISTIC PHILOSOPHY");
    record21.set("category", "security");
    record21.set("description", "Theoretical framework");
  try {
    app.save(record21);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record22 = new Record(collection);
    record22.set("label", "EVERYDAY SPEECH");
    record22.set("category", "security");
    record22.set("description", "Common usage");
  try {
    app.save(record22);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }

  const record23 = new Record(collection);
    record23.set("label", "MEMORIAL RECORDS");
    record23.set("category", "security");
    record23.set("description", "Memorial/grave documentation");
  try {
    app.save(record23);
  } catch (e) {
    if (e.message.includes("Value must be unique")) {
      console.log("Record with unique value already exists, skipping");
    } else {
      throw e;
    }
  }
}, (app) => {
  // Rollback: record IDs not known, manual cleanup needed
})